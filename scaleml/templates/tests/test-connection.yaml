apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "scaleml.fullname" . }}-test-connection"
  labels:
{{ include "scaleml.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "scaleml.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
